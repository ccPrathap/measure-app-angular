machine:
  node:
    version: 7.5.0
dependencies:
  pre:
    - npm install npm@latest -g
    - npm install angular-cli@latest -g
test:
  override:
    - npm run test
deployment:
  staging:
    branch: master
    heroku:
      appname: measure-app-angular