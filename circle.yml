dependencies:
  pre:
    - npm install npm@latest -g
    - npm install @angular/cli@latest -g
test:
  override:
    - karma start ./karma.conf.js
deployment:
  staging:
    branch: master
    heroku:
      appname: measure-app-angular