machine:
  node:
    version: 7.5.0
dependencies:
  pre:
    - npm install -g @angular/cli@latest
test:
  override:
    - npm run test
deployment:
  staging:
    branch: master
    heroku:
      appname: measure-app-angular